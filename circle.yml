machine:
  node:
    version: 4.2.3
  services:
    - docker

dependencies:
  pre:
    - npm install webpack
    - npm install babel-eslint
  post:
    - ./clusternator-scripts/clusternator-decrypt.sh

test:
  post:
    - ./clusternator-scripts/clusternator-docker-build.sh
    - ./clusternator-scripts/clusternator-decrypt.sh
    - node ./clusternator-scripts/clusternator-notify.js
